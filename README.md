how to start (mobile):
-npx i --force

-git clone https://github.com/Jans-YT/myProject.git

-npm install axios

-npm install -g expo-cli

-npm i tailwindcss --force

-npm start

"dikarenakan project mobile menggunakan expo sehingga bisa di jalankan dengan menggunakan aplikasi expo di
mobile android atau ios, tapi untuk di ios pasti akan terjadi beberapa bug sehingga di anjurkan menggunakan android untuk run nya. 
atau bisa juga menggunakan android studio untuk membuat viritual device"
